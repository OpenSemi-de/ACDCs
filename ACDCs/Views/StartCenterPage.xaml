<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:ACDCs.Components"
             xmlns:window="clr-namespace:ACDCs.Components.Window"
             x:Class="ACDCs.Views.StartCenterPage"
             Title="Main">
    <Grid HorizontalOptions="Fill" VerticalOptions="Fill">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="34"></RowDefinition>
        </Grid.RowDefinitions>
        <components:SharpAbsoluteLayout Grid.Row="0" x:Name="MainWindowLayout">
            <window:WindowStarterFrame x:Name="StarterFrame" AbsoluteLayout.LayoutFlags="YProportional" AbsoluteLayout.LayoutBounds="0,1,200,400"></window:WindowStarterFrame>
        </components:SharpAbsoluteLayout>
        <window:WindowTabBar StarterFrame="{x:Reference StarterFrame}" Grid.Row="1" x:Name="windowTabBar" HorizontalOptions="Fill" HeightRequest="42"></window:WindowTabBar>
    </Grid>
</ContentPage>
